<nav class="navbar navbar-light bg-secondary">
  <span class="navbar-brand text-white"><a class="text-white" href="/">Accueil</a></span>
  
  <ul class="navbar-nav ">
      <li class="nav-item active">
        
        {% if app.user %}         
          <a class="mr-3 text-white" href="{{ path('exercice_create') }}">Créer un exercice</a>
          <a class="mr-3 text-white" href="{{ path('exercice_program_add') }}">Ajouter un programme</a>
          <a class="mr-3 text-white" href="{{ path('exercice_program_show') }}">Mes programmes</a> 
          <a class="mr-3 text-white" href="{{ path('app_logout') }}">Déconnexion</a> 
          {% else %}
          <a class="mr-3 text-white" href="{{ path('app_login') }}">Connexion</a> 
          <a class="mr-3 text-white" href="{{ path('app_register') }}">Créer un compte</a> 
        {% endif %}
      </li>
    </ul>
</nav>
